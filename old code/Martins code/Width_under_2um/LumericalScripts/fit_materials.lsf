switchtolayout;
for(j=1;j<=num_materials;j=j+1){

commandstring = "mat_test = copymaterial(mat" + num2str(j)+");";
eval(commandstring);

setmaterial(mat_test,"max coefficients",max_coefficients);
setmaterial(mat_test,"tolerance",tolerance);
    
    
}